# SecretAgent — fresh-session handoff (2026-06-25)

> **Paste this whole file into a fresh session to continue.** It is self-contained.

## What this is

**SecretAgent** — a clean-room Rust reimplementation of Nous Research's Hermes Agent (MIT, no upstream
source copied): a self-hosted autonomous AI agent daemon shipped as a single self-contained binary, with
a security-first trust spine. Repo: `C:\Users\dnoye\ClaudeSecondBrain\SecretAgent` (private remote
`Vividness9816/secretagent-rust-agent-2026-06-19`, branch **`master`**, HEAD **`47c6c51`**).

## Where we are — the whole build is DONE

**Phases 0–6 are build-complete and CI-green.** Nothing is broken. P0 vault/audit → P1 memory+providers+
agentic loop → P2 tools+landlock sandbox+`Tainted<T>` injection guard+MCP → P3 learning loop (skills/user-
model/summarization) → P4 daemon+service+4 connectors (Telegram/Discord/Email/Slack)+NL→cron+trust spine →
P5 exec backends (Docker/SSH)+subagents+voice → P6 parity v1 (refactor, signed-release packaging, egress
seam, shell/op_tool, native Anthropic+model-switch, reedline TUI, backup/restore/export, self-update,
parity-tail doc). Read `PROGRESS.md` (slice ledger) + `ROADMAP.md` (phase map) for detail.

**Post-6 (this session):** `secretagent tui --yes` adds an **execute mode** to the interactive REPL
(`b4c307e`) — the default REPL denies side-effects; `--yes` auto-approves them for the session. `47c6c51`
gitignores the minisign signing key.

## What's been verified live (2026-06-25 operator session)

| Leg | State | How |
|---|---|---|
| **Slack connector** | ✅ PROVEN LIVE | DM the bot → `connector.accepted` → `tool.execute_code` (Docker `--network=none`) → reply in Slack. Live brain: `C:\Users\dnoye\secretagent`. Gateway runnable via `secretagent gateway`. |
| **SSH exec backend** | ✅ PROVEN | A privilege-free **userspace asyncssh server in WSL** is the remote (`~/sa-sshd/server.py` on `0.0.0.0:2222`; Windows `~/.ssh/config` alias `sa-wsl`). `execute_code` wrote a sentinel on the remote; backend pattern = `ssh <host> /bin/sh -s` with code on stdin. Test cfg `C:\Users\dnoye\sa-task2`. |
| **Voice** | ✅ PROVEN | whisper.cpp `whisper-cli -nt -np` + piper `en_US-amy-medium` (binaries in `C:\Users\dnoye\sa-voice-tools`). Round-trip in→"4"→`voice-out.wav`; reply wav re-transcribes to the answer. Test cfg `C:\Users\dnoye\sa-task3`. |
| **Native Anthropic** | ✅ plumbing-verified (needs key) | `kind=anthropic` reaches `api.anthropic.com` and returns 401 without a key (ignores `base_url`, fails closed). Test cfg `C:\Users\dnoye\sa-task4`. |

## The only real work remaining

1. **Finish the signed release (the last gated step).** The minisign keypair already EXISTS
   (`secretagent.key`/`secretagent.pub` in the repo root, key id `29D70D27708D0EAE`) and the **real**
   pubkey is already pinned in the **working tree** (`secretagent/src/self_update.rs` `PINNED_MINISIGN_PUBKEY_B64`
   + `install.sh` `MINISIGN_PUBKEY`) but **UNCOMMITTED** (HEAD has `""`; the install.sh `# TODO` comment is
   stale). To finish (do NOT re-run `minisign -G`): **commit the pin** (self_update.rs + install.sh) → add
   `secretagent.key` as the GH Actions secret **`MINISIGN_SECRET_KEY`** → bump `secretagent/Cargo.toml`
   `0.0.0`→`0.1.0` → `git tag v0.1.0 && git push origin v0.1.0` (fires `release.yml`) → set `[update] base_url`
   in config. See `docs/RELEASE.md`. **`secretagent.key` is now gitignored** — never commit it.
2. **Live Anthropic** (optional): `secretagent vault set ANTHROPIC_API_KEY "sk-ant-…"` in a brain configured
   `kind=anthropic`, then `secretagent run "…"`. Verify the key never lands in `audit.jsonl`.

## Optional / nice-to-have

- **`context.md` — the bot doesn't know the operator's projects.** SecretAgent reads `SOUL.md` + `context.md`
  from its config dir into the system prompt, but they're empty. Populate `C:\Users\dnoye\secretagent\context.md`
  with a project portfolio so `/secretagent` and the REPL have standing knowledge. (SecretAgent has its own
  SQLite brain; it does NOT see the Claude Code memory/vault.)
- **Slack read access** is intentionally OFF (the Slack connector's `allow_tools = ["execute_code"]`). Enabling
  `read_file` over Slack + a broad `read_roots` would let a remote sender exfiltrate secrets under the tree
  (including the signing key) — if wanted, scope a narrow `read_root` (a single project subdir) and add
  `read_file` to `allow_tools`.
- **Per-action approval in the REPL.** `tui --yes` is all-or-nothing (auto-approves every side-effect). A
  y/n-per-tool prompt would need an approval callback threaded through `sa-core::run_task` (the trust-spine
  loop) — a deeper change, not done.

## Environment + gotchas (read before debugging)

- **Local UX added this session:** PowerShell `secretagent` launcher (bare → `tui --yes` execute REPL; in the
  WindowsPowerShell profile), a `/secretagent <msg>` Claude Code slash command (`~/.claude/commands/secretagent.md`
  → `secretagent chat`), and `[policy] read_roots = ["C:/Users/dnoye/ClaudeSecondBrain"]` in the live brain
  config (local read access; Slack still can't read).
- **The prebuilt `target/release/secretagent.exe` can be STALE.** It was rebuilt to `sha8c5c9422` this session
  (the old one predated 6g/6h). If a subcommand is "unrecognized," rebuild: `cargo build --release -p secretagent`.
  **Stop the running gateway first** (it locks the exe): `Get-Process secretagent | Stop-Process -Force`, build,
  then restart `secretagent gateway`.
- **`hermes3` (the local default model) mis-narrates tool output** — it confabulated a fake hostname in one SSH
  run while the backend ran correctly. The backends are sound; for faithful result-relay use a cloud model.
- **Uncommitted WIP in the tree** (left intentionally): the release-pin edits to `self_update.rs` + `install.sh`,
  and the untracked public `secretagent.pub` (RELEASE.md says publish that one). The private `secretagent.key`
  is gitignored. **Stage deliberately — never `git add -A` until the key is confirmed gitignored.**
- **Brains are per-config-dir.** The live/Slack brain is `C:\Users\dnoye\secretagent`; `sa-task2/3/4` are
  isolated test brains. Set `SECRETAGENT_DATA_DIR` + `SECRETAGENT_CONFIG_DIR` to pick one.

## Pointers
`PROGRESS.md` (Post-6 + slice ledger) · `ROADMAP.md` (phases) · `docs/OPERATOR-SETUP-RUNBOOK.md` (the gated
tasks, now mostly done) · `docs/RELEASE.md` (cut a release) · `docs/parity-tail.md` (honest §4 parity map) ·
`~/.claude/second-brain/decisions/ADR-2026062*-secretagent-*.md` (architecture decisions).
