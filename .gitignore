/target
**/*.rs.bk
# never commit a real vault or identity
*.identity.age
/store.age
.secretagent/

# minisign signing SECRET (never commit; goes in GH secret MINISIGN_SECRET_KEY) + local logs
secretagent.key
*.log
